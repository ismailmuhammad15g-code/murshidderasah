# 🤖 نظام التحديث الآلي (Job Queue System)

## 📋 نظرة عامة

تم إضافة نظام تحديث آلي للبوت باستخدام **JobQueue** من مكتبة `python-telegram-bot` لضمان عمل البوت بشكل مستمر على السيرفر مع تحديث قاعدة البيانات تلقائياً.

---

## 🔄 المهام المجدولة

### 1️⃣ الباحث السريع (Quick Scraper)
- **التكرار**: كل 5 دقائق
- **التشغيل الأول**: بعد 10 ثوانٍ من تشغيل البوت
- **الوظيفة**: 
  - سحب الرسائل الجديدة من القناة (آخر 10 دقائق)
  - إضافة الرسائل الجديدة لقاعدة البيانات ChromaDB
  - تجنب التكرار (يتحقق من الرسائل الموجودة مسبقاً)

**الكود:**
```python
job_queue.run_repeating(
    scrape_new_messages,
    interval=300,  # 5 دقائق
    first=10,
    name="scraper_job"
)
```

---

### 2️⃣ عامل النظافة (Cleanup Worker)
- **التكرار**: مرة يومياً
- **الموعد**: 3:00 صباحاً (بتوقيت السيرفر)
- **الوظيفة**:
  - حذف الرسائل الأقدم من 365 يوم
  - تحرير مساحة التخزين
  - الحفاظ على أداء قاعدة البيانات

**الكود:**
```python
import datetime
job_queue.run_daily(
    prune_old_messages,
    time=datetime.time(hour=3, minute=0),
    name="cleanup_job"
)
```

---

## 📂 الملفات المعدلة

### 1. `scraper.py`
**الدالة الجديدة:**
```python
async def scrape_recent_messages(minutes: int = 10):
    """سحب الرسائل الحديثة فقط من القناة"""
```

### 2. `vector_store.py`
**الدوال الجديدة:**
```python
def add_new_messages(documents: list):
    """إضافة رسائل جديدة لقاعدة البيانات"""

def delete_old_messages(days: int = 365):
    """حذف الرسائل القديمة من قاعدة البيانات"""
```

### 3. `main.py`
**الدوال الجديدة:**
```python
async def scrape_new_messages(context: ContextTypes.DEFAULT_TYPE):
    """مهمة الباحث السريع"""

async def prune_old_messages(context: ContextTypes.DEFAULT_TYPE):
    """مهمة عامل النظافة"""
```

---

## ⚙️ التخصيص

### تغيير فترة التحديث:
```python
# تغيير من 5 دقائق إلى 10 دقائق
job_queue.run_repeating(
    scrape_new_messages,
    interval=600,  # 10 دقائق بدلاً من 5
    first=10,
    name="scraper_job"
)
```

### تغيير موعد التنظيف:
```python
# تغيير من 3 صباحاً إلى 2 صباحاً
job_queue.run_daily(
    prune_old_messages,
    time=datetime.time(hour=2, minute=0),  # 2:00 صباحاً
    name="cleanup_job"
)
```

### تغيير مدة حذف الرسائل القديمة:
```python
# في دالة prune_old_messages
deleted = vector_store.delete_old_messages(days=180)  # 180 يوم بدلاً من 365
```

---

## 📊 المراقبة والسجلات

### سجلات الباحث السريع:
```
🔍 بدء مهمة التحديث الآلي...
✅ تم سحب 3 رسالة جديدة
✅ تمت إضافة 3 رسالة جديدة
✅ تم تحديث قاعدة البيانات بـ 3 رسالة جديدة
```

أو:
```
🔍 بدء مهمة التحديث الآلي...
ℹ️ لا توجد رسائل جديدة
```

### سجلات عامل النظافة:
```
🧹 بدء مهمة تنظيف الرسائل القديمة...
🧹 بدء تنظيف الرسائل الأقدم من 365 يوم...
✅ تم حذف 45 رسالة قديمة
```

---

## 🚨 معالجة الأخطاء

النظام يحتوي على معالجة شاملة للأخطاء:

```python
try:
    # تنفيذ المهمة
except Exception as e:
    logger.error(f"❌ خطأ في مهمة التحديث الآلي: {e}")
```

إذا فشلت إحدى المهام، لن يتوقف البوت - سيسجل الخطأ ويحاول مرة أخرى في الموعد التالي.

---

## ✅ المزايا

1. **تحديث تلقائي**: لا حاجة لإعادة تشغيل البوت لتحديث قاعدة البيانات
2. **كفاءة**: استخدام Batch API للسرعة
3. **ذكي**: يتجنب إضافة الرسائل المكررة
4. **صيانة ذاتية**: يحذف الرسائل القديمة تلقائياً
5. **مراقبة سهلة**: سجلات واضحة لكل عملية

---

## 🔧 استكشاف الأخطاء

### المشكلة: المهام لا تعمل
**الحل:**
- تأكد من تشغيل البوت باستخدام `application.run_polling()`
- تحقق من السجلات بحثاً عن أخطاء

### المشكلة: الرسائل تُضاف مرتين
**الحل:**
- النظام يتحقق من `doc['id']` قبل الإضافة
- تحقق من دالة `add_new_messages`

### المشكلة: التنظيف لا يحذف الرسائل
**الحل:**
- تأكد من أن `metadata['date']` بصيغة ISO: `2024-01-15T10:30:00+00:00`
- تحقق من السجلات لمعرفة السبب

---

## 📝 ملاحظات مهمة

- ⚠️ **التوقيت**: موعد التنظيف (3 صباحاً) يستخدم توقيت السيرفر
- ⚠️ **الأداء**: سحب 10 دقائق كل 5 دقائق يضمن عدم تفويت رسائل
- ⚠️ **التخزين**: حذف الرسائل القديمة يحافظ على حجم قاعدة البيانات

---

## 📚 المراجع

- [python-telegram-bot JobQueue](https://docs.python-telegram-bot.org/en/stable/telegram.ext.jobqueue.html)
- [Google Embeddings API](https://ai.google.dev/api/embeddings)
- [ChromaDB Documentation](https://docs.trychroma.com/)
